# PROGRESS — program state machine

**Single source of truth for cross-session work.** If code and this file disagree, code wins — reconcile here and note it in SESSION_LOG.

**Working branch:** `develop` (holds `program/` state). **Releasable:** `main`. **Feature branches:** `wp-<id>-<slug>` → PR into `develop`. See DECISIONS ADR-001.
**Status legend:** TODO · IN-PROGRESS · BLOCKED · IN-REVIEW · DONE. **Scope tag:** `v1` (ship first) · `v1.x+` (designed now, delivered later) — ADR-002.

---

## ▶ RESUME HERE
**✅ v2.6.2 SHIPPED on ALL channels + 📘 v3 roadmap planned (S26).** `main` = `develop` = **2.6.2**, published & verified job-by-job: PyPI (OIDC) · GitHub Release (wheel/sdist/`.mcpb`/SBOM/cosign) · Homebrew tap bumped · **GHCR multi-arch `:2.6.2`+`:latest`** (release run 27900687385 + docker run 27900687379 both success). Owner also flipped the **GHCR package public** (anon `docker pull` works) and revoked the two pasted PATs. v2.6.2 was docs/metadata-only (cross-AI reframe of README + all 7 surfaces; identical CODE to 2.6.0/2.6.1). Live everywhere: GitHub README + repo nav, Claude marketplace, Glama, PyPI page, `.mcpb`, GitHub Release. **S26 = planning + 5-round adversarial review of the plan:** a full **v2.7 → v3.0.0 roadmap** is in **`program/ROADMAP_V3.md`**, now **ADVERSARIALLY CONVERGED** — 26 expert-agent reviews across 5 rounds (R1 10 lenses→~5 Critical; R2 7→4 Critical; R3 6→0 Critical; R4 edits; R5 3 lenses→CONVERGED). Hardened with cross-cutting contracts [C1]–[C6] (determinism incl. the env-nondeterminism gap, atomic-write, no-egress, supply-chain, migration, measurement), Effort/Risk tags, a v2.10 split for ⚠️ invariant-risk features, ADR-010 (dependency deprecate-then-remove), WP-100…WP-204 + WP-127/155/174/175/176/177/181a-b/193-fix/route, and measurable gates (KPI vs benchmark). **▶ NEXT (v3 program kickoff): start `v2.7.0` — "slim core (additive) + easy install"** per the release train. Recommended first PR = **WP-100/101/102** (atomic `.md` write [R-19] + per-script Brahmic normalization [R-16] + length-aware fuzzy [R-17]) — one small fully-deterministic, test-first change. Then WP-202a (eval corpus + CI baseline) and WP-181a (numpy-free, merge-decision-identical core). **Owner pre-reqs surfaced by review:** paid Windows Authenticode cert + Apple Developer ID (WP-175) for the no-block GUI installers; confirm the community-algo pin (v3.0). One residual non-code follow-up: the **GitHub repo "About" description + topics** is still Claude-only (no edit-repo tool available in-session; owner edit — suggested cross-AI text is in SESSION_LOG S25/S26). _Prior milestone below:_

**🔨 v2.6.0 (S24) — recall + resolve PERFORMANCE pass on `wp-26-perf-recall-resolve` (feature MINOR; CONVERGED).** Closed deferred backlog R-13/R-14/R-15 with no behaviour change (ranking byte-identical; resolve merges = brute-force parity), no new dependency, all invariants intact. **R-13:** digest writes a deterministic pre-tokenised `bm25_index.json`; recall ranks from it → **~8.8× faster @8k units** (additive, back-compat fallback). **R-15:** `store.load_meta` reads only `vectors.json` — recall never loads the embedding matrix. **R-14:** entity resolution blocks pairs by `(script, prefix-2 + suffix-2 of each token)` → **6.6% of O(n²)** pairs (true-full-scan parity-tested), dense n×n cosine dropped; silent 1500 cap → documented `MTA_RESOLVE_MAX_NAMES` (default 5000, 0=unbounded); WP-90 Bengali distinctness untouched. **CONVERGED — 2 clean rounds:** round 1 adversarial → 1 High (leading-char-edit merges dropped) + 1 Low fixed (+ true full-scan parity test added); round 2 independent fresh-verification → **NO Critical/High** (one Low doc over-claim → tightened). Bumped all 7 surfaces → **2.6.0** (check_versions OK); **261 pass / 3 skip**; wheel+sdist **twine PASSED**; full CI matrix green on PR #75 (run 27883514314). RISKS R-13/14/15 → Resolved. **Also bundled R-18 (supply-chain, WP-27):** release signing migrated to the single-file `*.sigstore.json` cosign bundle (cosign v3, off the v4-removed `.sig`/`.pem` form) — syntax verified vs Sigstore docs, exercises only on a real tag release (fail-safe). Remaining backlog now just **R-16/R-17** (non-Bengali Indic / short-Bengali fuzzy — *accepted*, English+Bengali scope). **▶ NEXT: PR #75 is converged + CI-green → human review/merge into `develop`; then on user go-ahead release v2.6.0 (FF main, owner tag-push `v2.6.0` — sandbox blocks tag pushes via HTTP-403; the R-18 cosign change first exercises on that tag run).**

**🔨 v2.5.0 (S23) — cross-AI multi-client auto-config built on `claude/plugin-review-improve-rqktks` (feature MINOR).** Closes the headline user-requirement gap (support + auto-configure Claude/Gemini/Grok/ChatGPT, all OSes). New `mta/core/clients.py` + `mta setup` registers the local stdio server into every *detected* MCP client — Claude Desktop/Code, Gemini CLI (`~/.gemini/settings.json`), Cursor (`~/.cursor/mcp.json`), VS Code (user `mcp.json`, `servers`+`type:stdio`), Windsurf (`~/.codeium/windsurf/mcp_config.json`), OpenAI **Codex** = ChatGPT's agent (`~/.codex/config.toml`, TOML append-or-create, no writer dep); **Grok Build** auto-discovers the Claude/.mcp.json config. ChatGPT app + xAI API are remote-MCP-only (documented `mta serve --http` path). All cross-AI config formats **verified against vendor docs** (3 research agents). Idempotent, per-file backup, JSONC + non-object-JSON clobber-safe, atomic-write reuse; install.sh + Windows launch.py wired; `updater._touch` race fixed. Bumped all 7 version surfaces → **2.5.0** (check_versions OK); **250 pass / 3 skip**; wheel+sdist **twine PASSED**; clients.py ships in the wheel. **CONVERGED — two consecutive clean review rounds** (round 1 adversarial → 1 High+Med+Low fixed; round 2 independent fresh-eyes → NO Critical/High; the last Low also fixed). PR #73 **MERGED → `main`** (`f32cc86`); full CI matrix green (Ubuntu/macOS/Windows × 3.10/3.12 + conversion-e2e + supply-chain, run 27882098568) and the release-build dry-run validated green (wheel/sdist/twine + `.mcpb` + SBOM + cosign keyless), publish jobs correctly tag-gated. **▶ NEXT (owner-only publish gate):** this sandbox blocks `git push` of tags (HTTP 403), so from an authenticated clone run `git tag -a v2.5.0 f32cc86 && git push origin v2.5.0` → fires release.yml (PyPI OIDC → GitHub Release+cosign+SBOM+.mcpb → Homebrew) + docker.yml (GHCR); then `mcp-publisher publish` for the MCP registry. Deferred Med/Low = RISKS R-13…R-19.**

**✅ v2.4.2 SHIPPED — S22 maximal re-audit → review-driven fixes → published & verified.** `main` = `develop` = **v2.4.2** (PR #72→main; annotated tag `v2.4.2` on `08731e0`; release train run 27869031283 success). **Fixed:** TF-1 (Critical, token-free byte-caps), TF-2/RES-1/SEC-1/WIN-1 (High) + Med bundle, each with a regression test (18 new). Converged: 2 clean review rounds + independent fresh-verification SHIP; no open Crit/High (CONVERGENCE.md). **Published channels verified:** PyPI 2.4.2 artifact LIVE (wheel installs + EN/Bengali smoke recall passes — installed via direct wheel URL; metadata API + HTML index list 2.4.2); GitHub Release **published** with wheel+sdist+.mcpb+SBOM, **all 4 cosign-verified** (keyless, release.yml@refs/tags/v2.4.2); Homebrew tap formula **v2.4.2**; `.mcpb` valid (darwin/linux/win32 + win32→`python launch.py`); Docker train run **success** (GHCR image pushed). **⚠ TRANSIENT at ship time (diagnosed, benign):** the release is HEALTHY — metadata API `yanked:False` + both files present (223503b whl / 248959b tgz), files.pythonhosted HTTP 200, and the **HTML** simple index lists 2.4.2 with sha256. ONLY PyPI's **PEP691 JSON** simple-index object (which modern pip prefers for `-U ==2.4.2`) was still stale ~25 min post-upload — a Warehouse/Fastly cache-object split between the HTML and JSON serializations, self-resolving on the next regeneration. NOT a bad artifact → **no yank**. Install works now via the wheel URL / GitHub Release asset; plain `pip install -U` resolves once the JSON index refreshes. **GHCR anon pull 404** (package private — owner follow-up to set public). **Real Windows .mcpb start** validated only in Claude Desktop (best-effort; not testable on this macOS host). **▶ NEXT: confirm `pip install -U memorised-them-all==2.4.2` resolves once the PyPI edge clears (artifact already live); then OWNER-ONLY: refresh MCP-registry server.json (`mcp-publisher`), marketplace/Glama listings, set GHCR public. Worthwhile backlog else exhausted; deferred Med/Low = RISKS R-13…R-19.** _Fix-loop record below:_

**🔨 v2.4.2 (S22) — maximal re-audit found real defects → review-driven fix loop (DONE).** A fresh **9-agent maximal review** (lenses A–I) re-audited the shipped v2.4.1 and found **1 Critical + 4 High** (full consolidation: `program/REVIEW.md`). Baseline was green first (215 pass/2 skip core lane; check_versions OK; determinism byte-identical re-confirmed via SHA-256). **Blocking set:** TF-1 (**Critical** — `recall`/`overview` never byte-clamp the `label` field; Bengali entity labels bypass the Latin length gate → **measured 394 KB recall + 157 KB overview** → token-free invariant violated), TF-2 (High — recall "byte caps" are actually *char* slices → 3× over for 3-byte Bengali), RES-1 (High — `resolve._norm` strips Bengali vowel-signs → merges distinct entities ভোলা≡ভালো / ঢাকা≡ঢাকি), SEC-1 (High — rar/7z path extracts fully to disk before any bomb cap → disk-fill DoS), WIN-1 (High — `.mcpb` runs `bash launch.sh`; Windows `launch.py` excluded → can't start on Windows though manifest claims win32). **WIN-1 user decision = "make it work (best-effort)"** (ship launch.py + Windows command override + bootstrap; not locally testable on Windows → CI + structural validation only). Target = **PATCH v2.4.2** (bug/security only). WPs minted: WP-89 (TF-1+TF-2), WP-90 (RES-1), WP-91 (WIN-1), WP-92 (SEC-1), WP-93 (Med code: render note-collision+clamp, _AUDIO_EXTS parity, cross-OS newline determinism, recall_min_score docstring, overlap-stop fallback), WP-94 (Med docs/de-stale: slash commands, README config table, Dockerfile/CI dead-env, stale LLM comments). Med perf refactors (BM25 index cache, resolve O(n²)) DEFERRED to backlog (workaround exists, not blocking). **▶ NEXT: implement WP-89 on `wp-89-token-free-bytecaps`, test-first, green local lanes → PR.** _Prior shipped milestone below:_

**✅ v2.4.1 SHIPPED — WP-88: README completely redeveloped + de-staled EVERY published surface (PR #70→develop, #71→main; tag v2.4.1; both venvs→2.4.1; GitHub description+topics refreshed).** Docs/metadata only, no behaviour change. A 6-agent surface audit found staleness concentrated in `CITATION.cff` (still advertised audio + "offline interactive mind map" + "local models"), the PyPI summary ("Tuned for Apple silicon") + `pyproject` keywords (`local-llm`/`mind-map`), and the GitHub description/topics (whisper/mind-map/ollama/local-llm/apple-silicon) — plus pinpoint defects (the dangling empty "explore visually" README step, `memory_status` "models", "embeddings"/"vectors" wording, `manifest.json` missing win32 + `forget` "vectors", dead `faster-whisper` dep). **Repo "cleanup" = a TEXT de-staling pass — the audit confirmed ZERO files are safe to delete (all live/test-wired).** Fixes: README rewritten (why-different pillars, install matrix incl. MCP registry, dedicated English & Bengali section, BM25/PII explainers); purged stale claims from CITATION.cff/pyproject/manifest/server.json/glama.json(+desc+keywords)/plugin/marketplace; de-staled /memorise & /memory-status commands, the memorise skill, install.sh/launch.sh headers, ACKNOWLEDGEMENTS (dropped Ollama/Whisper/MLX/Cytoscape; added Mukti+LibreOffice), removed dead faster-whisper dep, and stale docstrings in recall.py/platform.py/store.py/digest.py/server.py. **A 4-agent adversarial review verified accuracy (vs code) + cross-surface consistency + README links → caught one blocker (stale `pyproject` keywords) which was fixed; verdict ship.** 218 tests pass; release train completed/success (README republished to PyPI + GitHub Release). **NOTE:** earlier this session the Claude Desktop memory home was relocated OUT of iCloud to `~/.memorised-them-all` (iCloud on ~/Documents was duplicating/evicting files); portable bundles in `~/Downloads` (`upgp-memory-portable.zip` 6.9MB search-only, `upgp-memory-FULL.zip` 83MB with sources). _Prior milestone below:_

**✅ v2.4.0 SHIPPED — WP-87b: vetted Bengali reorder repair «রম্ন»→«রু» (PR #68→develop, #69→main; tag v2.4.0; both venvs→2.4.0).** The last deferred quality item, done safely. A PDF-font reorder artifact mis-encoded a u-kar as «রম্ন», leaving ~5.7k tokens of common Bengali UNSEARCHABLE (গ্রুপ stored as গ্রম্নপ, also শুরু/পুরুষ/করুন/গরু/দ্রুত/গুরুত্ব/জরুরি). A **3-lens Bengali-expert panel** (linguist/adversary/corpus-auditor × 4 candidate rules, adversarially verified vs all 96.8k corpus word-forms) approved EXACTLY ONE rule as orthographically-impossible-in-valid-Bengali → artifact-only: «রম্ন»→«রু» (র never abuts the ম্ন conjunct; genuine নিম্ন/সর্বনিম্ন PRESERVED). It **rejected 3 dangerous candidates** (kept deferred, need a dictionary/reposition logic): «ম্ন»→«ু» (নিম্ন), «েস্ন»→«্লে» (join "করে স্নান"→করেস্নান→কর্লোন), «ে্য»→«্য» (প্রত্যেক→প্রত্যক), «ণরে»→«ণের» (the -রে particle চরণরে/প্রাণরে). `bangla_legacy.normalize_reorder_artifacts` applies at **extraction** (so a **graph-only rebuild fixes an existing memory with NO re-conversion**) + at conversion (new files). **Graph-rebuilt + swapped both homes (backups `upgp.bak3-20260610-175002`):** রম্ন artifacts **0**, নিম্ন preserved (44), previously-unsearchable words now recall (গ্রুপ মিটিং→«এন্টারপ্রাইজ নির্বাচনের গ্রুপ মিটিং», গুরুত্বপূর্ণ/শুরু/পুরুষ/গরু/দ্রুত all hit), **0 doc regressions**, EN+BN+off-topic all good via the extension venv 2.4.0. +2 tests, **218 pass**. **USER: quit+reopen Claude Desktop (⌘Q) to load v2.4.0 + the updated memory.** _Prior milestone below:_

**✅ v2.3.0 SHIPPED — WP-87: expert-panel audit fixes → UPGP memory CONVERGED (PyPI/GitHub/Homebrew; PR #66→develop, #67→main; tag v2.3.0; both venvs upgraded to 2.3.0).** A 5-agent expert panel audited the COMPLETED memory (recall already strong); all FIX-NOW items shipped, deterministic/model-free: (1) **PII suppression** — survey rows (names+ages+phones) leaked into theme summaries/recall units (sit ~0.26–0.30 numeric, under the 0.55 gate); added a tabular/PII guard (pipe-rows + long Latin/Bengali digit-runs) on facts+summaries, `[number]` redaction, and a `_low_value` roster skip (also reclaimed ~210k survey-noise chunks → narrative fills the 25k budget). (2) **Bijoy PDF recovery** — the OOXML delegacifier was a no-op for PDFs, so Bijoy-ASCII text layers were Latin mojibake (a top theme unreadable); new **line-wise** `bangla_legacy.recover_mixed` (≥4 high-byte chars, ≥0.07 density, **no English function words**, conversion must yield real Bengali) recovers them while leaving English/Spanish/correct-Bengali untouched. (3) **Graph hygiene** — `Unnamed` blocklist, hex/XMP/base16 junk rejection, Bangladesh **gazetteer** typing (8 divisions+64 districts). (4) **Stopword-aware off-topic recall guard** (code-only, "best pizza"≠"Best Practices"). **Memory rebuilt (resumable driver, PYTHONPATH=repo) + swapped into both homes (backups `upgp.bak2-20260610-165134`):** converted **1628**, entities **32602**, communities **685**. **VERIFIED CONVERGED (via the extension venv 2.3.0 on the Desktop home, neutral cwd):** PII summaries **0**, `Unnamed`/hex-blob entities **0**, Bijoy-mojibake themes **0**, residual mojibake files **1**, recall **11/11 EN+BN** incl. the recovered Bengali («কর্মক্ষমতা হ্রাস… আঘাত» → «আঘাত সামলাতে পারেন»), off-topic declines, **0 doc regressions**. +20 tests; **216 offline pass**. **GOTCHA recorded:** the CLI venv had a STALE site-packages `mta` copy → a driver run-by-path imported it instead of edits; fixed by `PYTHONPATH=<repo>` for rebuilds + upgrading both venvs to published 2.3.0. **DEFERRED (optional, lower-value, all need a re-digest):** the panel's own deferrals — Unicode-Bengali reorder repair (ধরণরে→ধরণের) + conjunct glyph-alias (স্ন→ল্ল, HIGH corruption risk → needs a held-out correct-Bengali regression corpus first), 2-letter-acronym hygiene, coverage widening (25k cap), the 5 timeout `.xlsx` + 1 unparseable `.docx`. **USER: quit+reopen Claude Desktop (⌘Q) to load v2.3.0 + the converged memory.** _Prior milestone below:_

**✅ v2.2.0 SHIPPED — WP-84/85: Bengali-OCR recovery + BM25 recall (memory-quality convergence on the recall axis).** Builds on the v2.0.x deterministic model-free engine (no LLM/Ollama/embedding-model/GPU/network; 8 tools; security-hardened archive expander; content-hash dedup; media/font/junk skip; legacy binary Office via optional LibreOffice; Bengali/Unicode extraction + person/org/place typing + survey-noise suppression). **WP-84 (v2.1.0):** broken-font Bengali PDF auto-re-OCR (`_looks_like_broken_bengali` >15% orphaned vowel signs → Tesseract eng+ben). **WP-85 (v2.2.0):** recall is now a **BM25 lexical ranker** (was a hash-embedding cosine with no semantic/lexical signal) + the Bengali **halant-tokenisation bug** fixed (`_TOK=[\wঀ-৿]+` + NFC — bare `\w` split Bengali at ্ U+09CD → every Bengali query scored 0.0). **Recall reads existing memory ⇒ NO re-digest needed.** All on PyPI/GitHub/Homebrew; **both venvs (`~/.mta-app/venv` + Claude extension `.venv`) run 2.2.0 (editable from repo).** 201 offline tests pass; byte-identical deterministic. **UPGP memory** (real `~/.memorised-them-all` AND Desktop home `/Users/aninda/Documents/Claude/Memorised Files`, project `upgp`): 1479 docs, 25k chunks, ~27.9k entities, 519 themes. **VERIFIED on the live memory:** EN + Bengali queries hit (e.g. «ষাঁড় কেনার নির্দেশিকা» → «দরিদ্র পরিবার কে ষাঁড়»); genuinely off-topic queries → `low_confidence`/score 0 (declinable). Audit hit-rate ~5/12 → ~11/12. **✅ LEGACY-OFFICE TOP-UP DONE (WP-86, 2026-06-10):** user installed LibreOffice (26.2.4.2); a **resumable** full-source re-digest folded the legacy binary Office files in. **converted 1480→1625, failed 165→7, +136 new ok docs, 0 regressions** (live⊆staged superset). Verified live (extension venv 2.2.0 + Desktop home): legacy `.doc`/`.ppt` content now findable, EN+BN recall good, off-topic declines. Swapped into BOTH homes with `upgp.bak-20260610-151432` backups. Residual 7 non-legacy failures: 5 oversized modern `.xlsx` survey dumps (convert-timeout; mostly cell-noise the filters drop), 1 Word `~$` lock file (junk), 1 `.docx` markitdown can't parse; +3 unsupported `.eps/.indd/.nef` (design/photo, correctly skipped). **Resumable driver = `~/.mta-app/resumable_build.py`; staging corpus kept at `~/.mta-app/stage-upgp` (skip-base for any future top-up).** **⚠ Machine keeps killing long jobs** (killed this build at 1026/1900 + earlier LibreOffice/Homebrew/Ollama/.git) — owner to find the cleaner/AV/sync culprit. **DEFERRED (optional, all need a re-digest):** (a) the 5 timeout `.xlsx` + 1 unparseable `.docx` (raise `MTA_CONVERT_TIMEOUT`, low value); (b) ~5 residual broken-Bengali docs; (c) deeper extraction polish from the audit — entity typing (96.5% "other" → district/place gazetteer), theme/community-label junk filtering, fact-bearing entity ranking; (d) optional coverage widening (25k chunk cap truncates ~243k chunks). **USER: quit+reopen Claude Desktop (⌘Q) to load v2.2.0 + the topped-up UPGP memory.** Prior shipped state below:
**🔨 (superseded) earlier WP-80 note:** v2.0.0 deterministic, model-free, no-mindmap re-architecture + UPGP corpus. Owner: drop the local LLM + HTML mind map → pure-Python deterministic engine, **fully model-free (no Ollama at all)**, ship public v2.0.0; then convert+memorise `/Users/aninda/UPG Files` (legacy Bangla→Unicode, skip media, unarchive archives) into a `UPGP` memory; stress-test + verify-memory in loops with many agents. **Full spec: `program/V2_PLAN.md` (13 ordered steps, 8-tool set, archive/media-skip strategy, risks); corpus handling: `program/CORPUS_NOTES.md`.** A 10-agent scope workflow mapped the removal surface + surveyed the corpus (1714 content files, ~245 SutonnyMJ files, 6 zip+4 rar, 62 gdrive stubs to skip). Branch `wp-80-v2-deterministic` off develop. **Implementation underway** (steps 1-13: rip out LLM/Ollama/embedding-model/backends/lifecycle/mindmap; force hash embeddings; media-skip + recursive archive expander + content-hash dedup; 9→8 tools; config/manifest/docs/tests). Then stress + adversarial verify + ship v2.0.0 + corpus digest + memory verification. Prior shipped state below:
**🚢 v1.12.1 SHIPPED — WP-74 (docs/metadata refresh + Glama listing prep).** User: update README + product details on all platforms, then publish on glama.ai/mcp/servers. **Docs-only (no code):** README gained a **"Built to be reliable"** section (the 3 stress rounds in plain language) + **fixed the tools table** (said "eight", was missing `convert` → now all **nine**); refreshed descriptions on every surface (`.mcpb` manifest `long_description`, `server.json`, plugin/marketplace, GitHub repo description via API) to convey reliability hardening; added **`glama.json`** (`$schema` + `maintainers:["GRU-953"]`) to claim/maintain the server on the Glama MCP registry. Version→1.12.1 so PyPI/GitHub/Homebrew project pages refresh. PR #52→develop, #53→main, tag v1.12.1; full matrix green (build's `twine check` confirms README renders on PyPI). `main`=`develop`=v1.12.1. **OWNER ACTION (Glama):** sign in at glama.ai with GitHub (GRU-953) → open the server page → **Claim ownership** (org-vs-user n/a — GRU-953 is a User account, and `glama.json` is present either way). Built on:
**🚢 v1.12.0 SHIPPED — WP-73 (stress loop Round 3: lower-severity backlog cleanup).** User: "do round 3 and the remaining." Implemented all six tracked follow-ups (none a new crash/hang/corruption class), each with a regression test: (1) **0-byte file → status `empty`** (was `unsupported`/`failed`), any ext. (2) **BOM/UTF-16 decode** (`convert._decode_text_bytes`): Windows "Unicode" `.txt`/`.csv` no longer mojibake; an unknown-ext UTF-16 file no longer misflagged binary by its interleaved NULs. (3) **truncated-JSON salvage** (`extract._salvage_json_object`): recover complete entities/relations/facts from a cut-off small-model reply instead of dropping the chunk to classical (best-effort; None as before if unparseable). (4) **cgroup-aware `memory_gb`**: `min(host, cgroup v2/v1 limit)` so auto-tiering can't OOM inside a memory-capped container. (5) **out-of-tree symlink policy** in `_expand`: a folder-walk symlink whose target escapes the digested root is skipped (can't be tricked into reading `~/.ssh/…`); explicit paths honoured. **Adversarially reviewed before merge** → caught that a 6th attempted item (line-wise `_rearrange` perf opt) was NOT byte-identical to the Mukti oracle (one Pass-1 rule is position-gated, drags a kar across `\n` on ~0.17% of multi-line Bijoy) → **REVERTED it** (fidelity to the oracle-matched converter wins; the owner already converted their whole corpus); also fixed 2 nits (strip stray U+FEFF from utf-16/32 decode; restore `@lru_cache` on `memory_gb`). +9 tests; offline suite **211 pass/1 skip**. PR #50→develop, #51→main, tag v1.12.0; full matrix incl. conversion-e2e green. `main`=`develop`=v1.12.0. Reviewer verdict: **converged for a novice on ≤16 GB** across all 3 rounds; no Critical/High open. **Remaining backlog = essentially empty** (only the deferred semantics-preserving `_rearrange` linearization, perf-only, not worth the fidelity risk). Built on:
**🚢 v1.11.0 SHIPPED — WP-72 (honest degraded-mode reporting — stop the silent classical/hash fallback).** Round 2 of the stress loop; the #1 tracked follow-up and a roadblock the owner hit 3× (sessions 17/18/21): Ollama's launcher is reachable (`/api/tags` 200) but its runner is broken / model unpulled → every generate 500s → a digest **silently** degrades to classical/hash (once a 55-min run). **Now the tool says so, in 3 places:** (1) **`backends.inference_ok()`** — a real 1-token generate probe (not just `/api/tags`); **skips paid OpenAI-compatible backends** (never bills a remote endpoint). (2) **`memory_status`** — new `ollama_inference` (ok|degraded|down|disabled|unknown), top-level `degraded` bool, plain-English `health` line. (3) **`digest`** — top-level `degraded` (+ `degraded_reason`) when accurate mode was expected but the fallback ran; a **preflight probe** warns at the START, not after a long silent run. (4) **`recall`** — `memory_mode` (accurate|classical|fast). **Adversarially reviewed before merge** (1 expert agent) → caught a real cry-wolf bug (A1): `inference_ok` returned False for BOTH "broken" and "idle-stopped" (Ollama auto-stops after 5 min), so the preflight would false-alarm right before `ensure_running()` restarts it. **Fixed:** unreachable/idle → `None` (inconclusive, not broken); a slow cold model-load (timeout) → `None` not "degraded"; OpenAI backend → "unknown"; preflight warns only on a *definitive* break (`is False`). +9 tests (`test_degraded_mode.py`), full offline suite **202 pass / 1 skip**; PR #48→develop, #49→main, tag v1.11.0; full matrix incl. conversion-e2e green. `main`=`develop`=v1.11.0. Reviewer verdict: **converged** for a novice on ≤16 GB. **Round-3 backlog (next loop, all LOWER severity):** 0-byte→"empty"; UTF-16/BOM decode; truncated-JSON salvage; cgroup `memory.max` auto-detect; linear `_rearrange`; out-of-tree-symlink policy. Built on:
**🚢 v1.10.0 SHIPPED — WP-71 (stress-test hardening: no batch hangs, crash-safety, classical-path LLM safety).** User: stress-test the plugin every way (many expert agents), prioritise our roadblocks, loop to convergence. **Ran a 6-agent stress sweep that actively BROKE the plugin** (conversion chaos · runtime degradation · concurrency/crash · cross-platform/paths · LLM adversarial · resource limits) — all findings empirically reproduced. **Fixed the confirmed P0/HIGH cluster + 10 regression tests (191 green):** (1) **per-file conversion timeout** — each file in its own killable spawn subprocess (`MTA_CONVERT_TIMEOUT`, default 120 s, size-scaled) → one hung parser can't stall the batch forever (the #1 roadblock, was an infinite hang). (2) `_expand` skips symlink loops/unreadable dirs (os.walk no-follow) + **bounded & case-folded output filenames** (over-long name OSError / case collision no longer aborts or silently overwrites). (3) **crash-safety:** atomic `render.py` writes (memory.md/notes/mindmap); back up a corrupt `graph.json` before overwrite; corrupt `vectors.npz`→no_memory (was `BadZipFile` in recall); `setup-claude` unique temp. (4) **LLM safety on the DEFAULT classical path:** scrub control tokens + neutralise the prompt fence in `_classical` AND the summary/synopsis fallback (was LLM-path only); hardened `_scrub` (case-insensitive, fullwidth pipe, idempotent); entity cap + type-validate + **grounding** (drop hallucinations not in source). (5) **classical quality:** entity gating, sentence-scoped relations (was O(n²) clique), entity-bearing facts. (6) `MTA_MEMORY_GB` override (container RAM misreport); clamp negative MAX_FILE_MB; `bootstrap_path()` in workers. PR #46→develop, #47→main, tag v1.10.0; full matrix incl. conversion-e2e green. `main`=`develop`=v1.10.0. **Remaining stress backlog (next loop):** honest degraded-mode reporting (memory_status/recall/digest — the silent classical/hash fallback); truncated-JSON salvage; UTF-16/BOM decode; cgroup auto-detect; linear `_rearrange`; out-of-tree-symlink policy. Built on:
**🚢 v1.9.0 SHIPPED — WP-70 (default → 4 GB/no-GPU `micro` profile + guardrails from a 6-expert review).** User asked to brainstorm stability+accuracy guardrails across platforms with many expert agents (prioritising our roadblocks), default the plugin to a 4 GB no-GPU machine (users opt up), headless + novice-friendly. **Ran 6 parallel expert agents** (resource sizing · Ollama reliability · conversion robustness · cross-platform · LLM safety · novice UX) → strongly-convergent, code-level findings. **Implemented the top set:** (1) **NEW DEFAULT profile `micro`** — safe on 4 GB/no-GPU: classical extraction (no heavy LLM → can't OOM/thrash) + tiny `qwen3-embedding:0.6b` (hash fallback) + vision off + 1 worker; a digest always COMPLETES on any box. (2) **One-knob opt-up:** `MTA_PROFILE=auto` sizes to the machine (`platform.detect_tier()`: micro/small/standard/large); `standard`/`large` or any `MTA_*_MODEL` env overrides; `.mcpb` gains a "Performance profile" dropdown (default `auto`); `.mcp.json`→`MTA_PROFILE=auto`. (3) `worker_count` clamps to 1 on <6 GB. (4) **LLM-safety:** scrub special tokens on the SUMMARY+synopsis path too (prior fix was entities/facts only) + broadened the token set (gemma/llama/ChatML). +8 tests, 181 green; PR #44→develop, #45→main, tag v1.9.0. `main`=`develop`=v1.9.0. **Tracked follow-ups from the review (chips/notes):** cross-platform per-file convert timeout; honest "degraded mode" reporting in memory_status/recall (the silent classical/hash fallback); richer classical extractor; atomic render.py writes; deeper Ollama inference-probe. Built on:
**🚢 v1.8.0 SHIPPED — WP-69 (legacy Bengali SutonnyMJ→Unicode + convert-to-Markdown feature + LLM hardening).** Driven by the real **FY 25-26** corpus (2 GB, 711 files, heavy SutonnyMJ), iterated to convergence. **(1) Legacy Bengali → Unicode:** new `mta/core/bangla_legacy.py` — a faithful pure-Python port of the Mukti converter (MIT; 21/21 matched the JS oracle), with mapping data in `_bangla_maps.py`. **Font-aware** for Office (`.docx`/`.pptx`/`.xlsx`): converts only Bijoy-family (SutonnyMJ + 114 fonts) runs, so mixed English+Bengali docs convert cleanly (verified on a real docx: 7273 Bengali chars, 0 residual mojibake, English untouched); density-gated fallback for plain text never touches English. Wired into `convert.py` (default-on, `MTA_BANGLA_LEGACY`). **(2) `convert` tool/CLI:** convert-to-Markdown is now a first-class feature — `mta convert <paths> [--out DIR]` + MCP `convert()` (now **9 tools**, all surfaces in lockstep, count derived dynamically). **(3) LLM hardening (found via real Bengali):** strip qwen3 `<tool_call>`/ChatML/`<think>` tokens from extracted entities/facts (26 leaks → 0); `num_predict` 700→1024 + `<think>`-block parser stripping. **Live-validated:** real FY 25-26 digest = accurate mode, real embeddings, 0 leaks, relevant Bengali recall; 175 offline tests + full CI green (PR #42→develop, #43→main, tag v1.8.0). `main`=`develop`=v1.8.0. *(Note: a degraded/thrashed Ollama mid-run once forced a classical/hash fallback + 55-min wall-clock; a restart restored accurate mode — local LLM extraction on dense docs is inherently slow, use `--fast` for bulk.)* Built on:
**🚢 v1.7.0 SHIPPED — WP-68 (default model stack re-tuned to the optimum for ≤16 GB).** User asked to research (agents, repeatedly, live sources only) the latest+optimum ≤16 GB models and update the plugin, then test/fix/improve/publish. Two rounds (Round 1 = 3 parallel role agents; Round 2 = verifier against the authoritative `registry.ollama.ai` manifest API) → new defaults: extract `qwen2.5:7b`→**`qwen3:4b-instruct`** (2.5 GB), embed `nomic-embed-text`→**`qwen3-embedding:0.6b`** (0.64 GB, 1024-d), vision `moondream`→**`qwen3-vl:4b-instruct`** (3.3 GB), whisper `base`→**`small`**. Pinned `-instruct` (bare `qwen3:4b`/`qwen3-vl:4b` are *thinking* builds). Added a `qwen3-embedding` query-prefix. Old defaults kept as documented alternatives; all overridable. **Live-verified on a 16 GB Mac before release:** accurate digest with the new stack; **Bangla→Bangla recall 0.751**; embed dim 768→1024 degrades gracefully to lexical recall (no crash) + re-digest restores vectors. 166 offline tests + full CI green (PR #40→develop, #41→main, tag v1.7.0 run 27069875217). `main`=`develop`=v1.7.0. **Correction to the v1.6.3 note below:** `gemma4` and `qwen3.5` are now REAL (released since), confirmed via the registry manifest API — they're documented as newest/experimental options (defaults stay on the stable instruct builds). Built on:
**🚢 v1.6.3 SHIPPED — WP-67 (lighter/multilingual model alternatives, documented).** User asked if `gemma4:e2b-it-qat` is a better fit — at the time it wasn't a real tag (Gemma **3n**=`e2b-it`, Gemma **3**=`*-it-qat`); it has since become real (see the v1.7.0 correction above). Added opt-in alternatives to the README ("Choosing a model") + the `.mcpb` `manifest.json` field descriptions (defaults unchanged): extraction `gemma3:4b-it-qat`/`gemma3n:e2b-it-q4_K_M`/`gemma3:1b-it-qat`/`qwen2.5:3b`; embeddings `bge-m3` (multilingual — best for Bangla recall)/`mxbai-embed-large`; vision `llama3.2-vision`/`qwen2.5vl`/`granite3.2-vision`. Docs/manifest only, no code change (PR #38→develop, #39→main, tag v1.6.3 run 27068301458, 4/4 green). `main`=`develop`=v1.6.3. Built on:
**🚢 v1.6.2 SHIPPED — WP-66 (atomic `setup-claude` write + plugin `.mcp.json` eng+ben).** While verifying the user's install, `setup-claude`'s non-atomic `write_text` + non-dict-`mcpServers` no-op let a **running Claude Desktop revert the freshly-written entry**; fixed with a staged temp→`os.replace` write + dict coercion (PR #36→develop, PR #37→main, tag v1.6.2 = run 27038666401, 4/4 green; +1 test). Verified end-to-end: the 1.6.2 atomic write makes the Desktop `mcpServers` entry stick even with Desktop running. Built on:
**🚢 v1.6.1 SHIPPED — WP-65/65b (the user-reported `${HOME}` digest bug + requested defaults).** **v1.6.0**: fixed the `${HOME}` digest-blocker (literal unexpanded `MTA_HOME` → `config_file` null → digest failed) via robust path resolution (`config._resolve_home`); **eng+ben OCR** default (graceful pack-drop); **`mta setup-claude`** auto-config (run by `install.sh`); plus the convert-level all-types text-fallback. **v1.6.1**: completed **digest all file types** — the folder walk (`_expand`) now includes unknown extensions (`MTA_DIGEST_ALL` default on; binaries skipped, hidden skipped). MLX + Ollama-by-default retained. Both released (runs 27032556494 / 27033631662, 4/4 green) — PyPI 1.6.1, GitHub Release, tap, GHCR. **Fix verified end-to-end on the published package** (literal `${HOME}` → config_file populated + folder digest picks up `code.pyx`). `main` = `develop` = v1.6.3; no Critical/High open.
**⚠ User-machine file-deletion problem (S17–S18) — recurring:** three things vanished from the user's Mac mid-session — (1) the repo `.git` (re-attached from origin, no loss); (2) the entire Homebrew `mta` install (→ moved to a self-contained venv `~/.mta-app/venv`); (3) **Ollama's `llama-server` runner** (launcher survived so `/api/tags`/`is_up()` worked + models listed, but ALL inference 500'd → MTA correctly fell back to classical/hash). A clean `brew reinstall ollama` was still missing the runner → switched to the **self-contained cask `ollama-app`** (Ollama.app in /Applications, not the Cellar), symlinked `/opt/homebrew/bin/ollama` → the app binary; inference verified (embed 1×768 + qwen2.5 gen + a live MCP digest `mode: accurate, embed: ollama`). Also installed the **`[mlx]` extra** (`gpu: mlx`, `mlx_whisper: True`) and removed the stale broken `.mcpb` (registry + folder, recoverable). Venv now **1.6.3[mlx]**; both Claude configs point at it (atomic write). The plugin's "always use Ollama by default" is correctly implemented — the defect was the corrupted local Ollama, not MTA. **The user must find what is deleting files** (cleaner/optimizer/antivirus, aggressive sync, or disk faults — Disk Utility → First Aid). Recovery steps in `~/.mta-app/SETUP_NOTES.md`.
**(Earlier) v1.5.x:** Phase-3 interop (WP-20–24) + backlog (WP-60–63) + novice README (v1.5.1) + PIPE-06 (v1.5.2).
**Delivered this arc (S16):** Phase-3 — WP-20 HTTP transport (#19) · WP-21 schema exports (#20) · WP-22 REST gateway (#21) · WP-23 pluggable backends (#22) · WP-24 recipes + conformance (#23). Backlog — WP-60 supply-chain + CI-09 lockfile + release tag-gate (#25) · WP-61 Docker/GHCR (#26) · WP-62 vector-store consistency + PIPE-05 (#27) · WP-63 MCP-registry `server.json` (#28). All additive + invariant-safe.
**Owner follow-ups (one-time, NOT blocking):** rotate `HOMEBREW_TAP_TOKEN` (the S14-exposed PAT still works but should be replaced); submit the registry manifest once (`mcp-publisher login github && mcp-publisher publish`); optionally set the GHCR package public (for anonymous `docker pull`).
**▶ Next: NOTHING required — worthwhile backlog exhausted.** What deliberately remains, with rationale: full graph+vectors **write-transaction** *(deferred by design — a torn store is already safe via the load guard; it's a durability nicety vs. a risky store-layout + migration change, best done in its own focused effort)*; **RECALL-02** (classical facts are verbatim, provenance-tagged sentences — the designed recall behavior) + **LIFE-02** residual (mitigated in WP-14) — accepted/documented; **directory/marketplace listings** + token rotation + registry submit + GHCR-public — owner-only/external; winget/scoop — N/A for a pip tool (`PUBLISH_MANIFEST.md`).
**⚠ Concurrency (S16):** run ONE unattended session per working tree (or give each its own `git worktree`).

---

## Work Packages

| id | title | phase | scope | status | branch/PR | updated | next-action |
|----|-------|:-----:|:-----:|--------|-----------|:------:|-------------|
| WP-00 | Program setup (state files, branch conventions) | 0 | v1 | **DONE** | develop | 06-02 | — |
| WP-01 | Deep audit → AUDIT.md | 1 | v1 | **DONE** | develop | 06-02 | — |
| WP-02 | Plan + risks + acceptance (**plan gate**) | 1–2 | v1 | **DONE** | develop | 06-02 | approved (S02) |
| WP-03 | CI fidelity + single version source + quick-win hygiene | 2 | v1 | **DONE** | merged #5 → develop (4548d02) | 06-02 | CI green; deferred PIPE-04/DOC-21/PKG-06 |
| WP-10 | Install simplicity + **offline-correct bootstrap** (R1) | 2 | v1 | **DONE** | merged #6 → develop (fd2d1d2) | 06-02 | PKG-03 closed; PKG-04 (Low) deferred |
| WP-13 | Safe auto-update: integrity+atomic+rollback (R4) | 2 | v1 | **DONE** | merged #6 → develop | 06-02 | SEC-04/DEP-01/03/09 closed; DEP-02 report-only (ADR-009) |
| WP-14 | Lifecycle + **cross-process concurrency** (R5) | 2 | v1 | **DONE** | merged #7 → develop (a5851ab) | 06-02 | LIFE-01/PIPE-03/DEP-08 closed; LIFE-02 improved |
| WP-15 | Compatibility / versioning / **data migration** (R6) | 2 | v1 | **DONE** | merged #8 → develop (90cfffd) | 06-02 | LIFE-03 closed; A7 met |
| WP-11 | Auto-configuration: profiles, persist, GPU/LM-Studio (R2) | 2 | v1 | **DONE** | merged #9 → develop (81bf1c0) | 06-02 | DEP-05/06/07 closed |
| WP-12 | Dependency scan + guided install + `mta doctor` (R3) | 2 | v1 | **DONE** | merged #10 → develop (66ca5d6) | 06-02 | DEP-04/10 closed; **Phase 2 ✅** |
| WP-30 | Offline recall reliability + classical quality | 4 | v1 | **DONE** | merged #11 → develop (84e8c6c) | 06-02 | DOC-01/RECALL-03 closed (A4); RECALL-02/PIPE-05/06 deferred (Med) |
| WP-32 | Security hardening completion + SECURITY.md | 4 | v1 | **DONE** | merged #12 → develop (6c52714) | 06-02 | SEC-01/02/03/10/11 closed (A12) |
| WP-31 | Eval harness + reference corpus + golden metrics | 4 | v1 | **DONE** | merged #13 → develop (24aef47) | 06-02 | recall@8 gated; DOC-18/19 reworded (A10 partial/A11 reported) |
| WP-33 | Quick-win sweep (PIPE-04, DOC-21, PKG-04) | 4 | v1 | **DONE** | merged #15 → develop (d511a0d) | 06-02 | PKG-06 n/a; CI-09 deferred; RECALL-02/LIFE-02 noted |
| WP-34 | Pre-release fresh-eyes review + fixes | 6 | v1 | **DONE** | merged #16 → develop (a46a414) | 06-02 | 21 findings; 3 High + Med/Low fixed; `program/REVIEW.md` |
| WP-40 | Release train + supply-chain + publish manifest | 5 | v1 core / v1.x+ rest | **DONE** | merged #14 → develop (abca304) | 06-02 | CI-02/03/04/05/06/11, SEC-06/07 closed; CI-09 lockfile deferred |
| WP-41 | First synchronized v1 release | 5 | v1 | **DONE 🚢** | v1.4.0 tag (run 26835623380) | 06-02 | PUBLISHED: PyPI + GitHub Release + .mcpb + Homebrew tap; smoke ✓ |
| WP-50 | Sandbox/E2E harness | 6 | v1 | **DONE** | merged #17 → develop | 06-02 | clean-wheel CLI E2E + e2e.yml; container matrix deferred (R-01) |
| WP-51 | E2E test-matrix run | 6 | v1 | **DONE** | merged #17 | 06-02 | offline 5/5 + accurate-mode pass (live Ollama) |
| WP-52 | Fix-and-retest loop → TEST_REPORT.md | 6 | v1 | **DONE** | merged #17 | 06-02 | `program/TEST_REPORT.md` |
| WP-90 | Convergence review & note | 6 | v1 | **DONE** | develop | 06-02 | `program/CONVERGENCE.md` — converged (code) |
| WP-20 | Phase-3: secure Streamable HTTP transport (stdio + HTTP) | 3 | v1.x+ | **DONE** | merged #19 → develop (9e1029a) | 06-03 | `serve --http`; loopback+bearer+DNS-rebind; no new dep |
| WP-21 | Phase-3: cross-AI schema exports (OpenAI/Gemini/OpenAPI 3.1) | 3 | v1.x+ | **DONE** | merged #20 → develop (fa86ec3) | 06-03 | `export-schema`; derived from live registry (no drift) |
| WP-22 | Phase-3: local REST gateway over the OpenAPI-3.1 surface | 3 | v1.x+ | **DONE** | merged #21 → develop (2cf269b) | 06-03 | `serve --rest`; bearer+Host-allowlist; threadpool |
| WP-23 | Phase-3: pluggable backends (Ollama/LM Studio/llama.cpp/OpenAI-compat) | 3 | v1.x+ | **DONE** | merged #22 → develop (07e6d96) | 06-03 | `MTA_BACKEND`; Ollama default byte-identical; fallback intact |
| WP-24 | Phase-3: per-client recipes + conformance tests | 3 | v1.x+ | **DONE** | merged #23 → develop (12ba7ac) | 06-03 | `mta recipes`; all surfaces == same 8 tools |
| WP-60 | Supply-chain: lockfile (CI-09) + pip-audit/license + release tag-gate | 5 | v1.x+ | **DONE** | merged #25 → develop | 06-03 | `constraints.txt`; dispatch=dry-run; tap best-effort |
| WP-61 | Docker image + GHCR multi-arch publishing | 5 | v1.x+ | **DONE** | merged #26 → develop | 06-03 | `Dockerfile` + `docker.yml`; GITHUB_TOKEN, no secret |
| WP-62 | Robustness: vector-store consistency + rapidfuzz hard-dep (PIPE-05) | 4 | v1.x+ | **DONE** | merged #27 → develop | 06-03 | `store.clear_vectors`; vectors-before-graph |
| WP-63 | MCP registry manifest (`server.json`) | 5 | v1.x+ | **DONE** | merged #28 → develop | 06-03 | version-gated; owner submits via `mcp-publisher` |
| WP-41b | Second synchronized release (v1.5.0) | 5 | v1.x+ | **DONE 🚢** | tag v1.5.0 (run 26844874577) | 06-03 | PyPI + GitHub Release + .mcpb + tap + GHCR; smoke ✓ |
| WP-64 | Classical-extractor quality (PIPE-06) | 4 | v1.x+ | **DONE** | merged #30 → develop | 06-03 | leading-determiner merge; clean facts; abbrev-aware split |
| WP-41c | README release (v1.5.1) + PIPE-06 release (v1.5.2) | 5 | v1.x+ | **DONE 🚢** | tags v1.5.1 / v1.5.2 | 06-03 | novice README live on PyPI/GitHub; smoke ✓ |
| WP-65 | `${HOME}` digest fix · eng+ben OCR · all-types (convert) · `mta setup-claude` | 2/4 | v1.x+ | **DONE 🚢** | merged #32; tag v1.6.0 (run 27032556494) | 06-06 | digest-blocker fixed + verified; 16 tests; install.sh auto-registers Claude |
| WP-65b | Digest all file types in folder walks (`_expand` + `MTA_DIGEST_ALL`) | 2 | v1.x+ | **DONE 🚢** | merged #34; tag v1.6.1 (run 27033631662) | 06-06 | folder walk includes unknown exts; verified code.pyx picked up; +4 tests |
| WP-66 | Atomic `setup-claude` write + non-dict `mcpServers` coercion; plugin `.mcp.json` eng+ben | 2 | v1.x+ | **DONE 🚢** | merged #36→develop, #37→main; tag v1.6.2 (run 27038666401) | 06-06 | running-Desktop revert fixed (temp→os.replace); +1 test; venv→1.6.2[mlx]; Ollama repaired via `ollama-app` cask |
| WP-67 | Document lighter/multilingual model alternatives (extraction/embed/vision) | 4 | v1.x+ | **DONE 🚢** | merged #38→develop, #39→main; tag v1.6.3 (run 27068301458) | 06-06 | README "Choosing a model" + manifest descriptions; gemma3:4b-it-qat / gemma3n:e2b-it / bge-m3 (Bangla); defaults unchanged; docs-only |
| WP-68 | Re-tune DEFAULT model stack to the optimum for ≤16 GB (qwen3 family) | 2/4 | v1.x+ | **DONE 🚢** | merged #40→develop, #41→main; tag v1.7.0 (run 27069875217) | 06-07 | qwen3:4b-instruct / qwen3-embedding:0.6b / qwen3-vl:4b-instruct / whisper small; agent research verified vs registry manifest API; live-tested (Bangla recall 0.751; dim-change graceful); +qwen3-embedding query-prefix; 166 tests green |
| WP-69 | Legacy Bengali (SutonnyMJ)→Unicode + `convert`-to-Markdown feature + LLM token hardening | 1/2/4 | v1.x+ | **DONE 🚢** | merged #42→develop, #43→main; tag v1.8.0 | 06-07 | `bangla_legacy.py` (Mukti port, 21/21 oracle, font-aware Office); `convert` CLI+MCP tool (9 tools); strip qwen3 `<tool_call>` leak (26→0); num_predict 700→1024; +9 tests, 175 green; driven by real FY 25-26 corpus |
| WP-70 | Default → 4 GB/no-GPU `micro` profile + 6-expert guardrail review | 2/4 | v1.x+ | **DONE 🚢** | merged #44→develop, #45→main; tag v1.9.0 | 06-08 | default `micro` (offline/classical, safe on any box) + `MTA_PROFILE=auto` tiering (`detect_tier`); worker floor→1 on <6 GB; scrub tokens on summary/synopsis too; .mcpb "Performance profile" dropdown; +8 tests, 181 green; deeper guardrails tracked as follow-ups |
| WP-71 | Stress-test hardening (6-agent break-it sweep, Round 1) | 2/4/6 | v1.x+ | **DONE 🚢** | merged #46→develop, #47→main; tag v1.10.0 | 06-08 | per-file convert timeout (killable spawn subprocess, `MTA_CONVERT_TIMEOUT`) → no infinite batch hang; symlink-loop-safe `_expand` + bounded/case-folded output names; atomic `render.py`, corrupt-graph backup, corrupt-`vectors.npz`→no_memory; classical-path scrub+fence+grounding+entity-cap; sentence-scoped relations; `MTA_MEMORY_GB`; +10 tests, 191 green; full matrix incl. conversion-e2e; backlog → next loop |
| WP-72 | Honest degraded-mode reporting (stress loop Round 2) | 4/6 | v1.x+ | **DONE 🚢** | merged #48→develop, #49→main; tag v1.11.0 | 06-08 | `backends.inference_ok()` real 1-token probe (skips paid backends); `memory_status` `ollama_inference`+`degraded`+`health`; `digest` `degraded`(+reason)+preflight early-warning; `recall` `memory_mode`; **adversarially reviewed → fixed cry-wolf A1** (idle-stopped≠broken → `None`; cold-load timeout→`None`; OpenAI→unknown; warn only on definitive break); +9 tests, 202 green; reviewer verdict = converged for novice ≤16 GB |
| WP-73 | Stress loop Round 3 — lower-severity backlog cleanup | 2/4/6 | v1.x+ | **DONE 🚢** | merged #50→develop, #51→main; tag v1.12.0 | 06-08 | 0-byte→`empty`; BOM/UTF-16 decode (`_decode_text_bytes`); truncated-JSON salvage (`_salvage_json_object`); cgroup-aware `memory_gb` (min host/limit); out-of-tree symlink policy in `_expand`; **adversarially reviewed → REVERTED a non-oracle-identical `_rearrange` opt** + fixed U+FEFF/`lru_cache` nits; +9 tests, 211 green; reviewer = converged across all 3 rounds |
| WP-74 | Docs/metadata refresh + Glama listing prep | 5 | v1.x+ | **DONE 🚢** | merged #52→develop, #53→main; tag v1.12.1 | 06-08 | README "Built to be reliable" + tools table fix (8→9, +`convert`); refreshed descriptions on `.mcpb`/`server.json`/plugin/marketplace/GitHub; added `glama.json`; docs-only, `twine check` green. Owner: claim on glama.ai via GitHub sign-in |
| WP-80 | v2.0.0 deterministic model-free re-architecture (drop LLM/Ollama/embed-model/vision/whisper + HTML mind map) | 1/2/4 | v2 | **DONE 🚢** | tags v2.0.0→v2.0.2 | 06-09 | 13-step rebuild per `V2_PLAN.md`; deleted `backends.py`/`lifecycle.py`; 256-d md5 hash embeddings; classical-only extraction; 9→8 tools (`open_mindmap` removed); byte-identical (no wall-clock in graph.json); stress + adversarial review |
| WP-81 | Security-hardened recursive archive expander + content-hash dedup + media/font/junk skip | 2 | v2 | **DONE 🚢** | tag v2.0.x | 06-09 | `archive.py` (zip/tar/gz/bz2/xz native; rar/7z via unar/7z); Zip-Slip/tar-symlink guards, bomb/entry/depth caps, all-or-nothing rollback; `_skip_category` (MTA_SKIP_MEDIA/FONTS/GDRIVE/JUNK); PK-magic Office/zip sniff |
| WP-82 | Classical extraction quality (Bengali/Unicode entities, person/org/place typing, survey-noise suppression) | 4 | v2 | **DONE 🚢** | tags v2.0.x→v2.0.3 | 06-09 | `_BN_RE`/`_BN_STOP`; `_VALUE_STOP(_LC)` case-insensitive demographic blocklist; `_low_value` numeric-dump skip; sentence-initial junk suppression; drops Male/No/Husband/NaN noise |
| WP-83 | Build the UPGP memory from `/Users/aninda/UPG Files` (legacy Bangla→Unicode, unarchive, skip media) | 2/4 | v2 | **DONE** | real `~/.memorised-them-all` + Desktop home; project `upgp` | 06-09 | 1479 docs, 25k chunks, ~27.9k entities, 519 themes; relocated to `/Users/aninda/Documents/Claude/Memorised Files` for Desktop |
| WP-84 | Broken-font Bengali PDF auto-re-OCR recovery | 4 | v2 | **DONE 🚢** | merged #62; tag v2.1.0 | 06-10 | `_looks_like_broken_bengali` (>15% orphaned vowel signs → `_ocr_pdf` eng+ben; method `+bn-ocr-recover`); recovered 73 docs (5 residual) |
| WP-85 | BM25 lexical recall + Bengali halant-tokenisation fix (recall-axis convergence) | 4 | v2 | **DONE 🚢** | merged #64→develop, #65→main; tag v2.2.0 | 06-10 | recall.py BM25 over (label·2+text), model-free; `_TOK=[\wঀ-৿]+`+NFC fixes `\w`-splits-at-halant (Bengali 0.0→hits); floor on BM25 scale; +3 tests, 201 green; **NO re-digest** (recall reads existing memory); audit hit-rate ~5/12→~11/12; both venvs editable→2.2.0; live-verified EN+BN on real UPGP memory |
| WP-86 | Legacy-Office top-up (LibreOffice now installed) → UPGP memory completed | 2/4 | v2 | **DONE** | resumable rebuild; swapped both homes | 06-10 | user installed LibreOffice; resumable full-source re-digest (`~/.mta-app/resumable_build.py`, skips already-converted) folded legacy `.doc/.ppt/.xls` in: converted 1480→1625, failed 165→7, +136 OK, 0 regressions; machine killed 1st run at 1026/1900 → resumable design; backups `upgp.bak-…` |
| WP-87 | Expert-panel audit of the completed memory → fix to convergence | 4 | v2 | **DONE 🚢** | merged #66→develop, #67→main; tag v2.3.0 | 06-10 | 5-agent panel; deterministic fixes: PII suppression (facts/summaries/`_low_value`) + `[number]` redaction; **line-wise** Bijoy PDF recovery (`recover_mixed`: ≥4 high-byte/≥0.07/no-English-funcwords/yields-Bengali); `Unnamed`+hex/XMP junk rejection; BD gazetteer typing; stopword-aware off-topic guard. Rebuilt+swapped (1628 docs, 32.6k entities); CONVERGED: PII/Unnamed/hex/mojibake themes all **0**, recall 11/11 EN+BN, 0 regressions; +20 tests, 216 pass; both venvs→2.3.0 |
| WP-87b | Vetted Bengali reorder repair (last deferred item) | 4 | v2 | **DONE 🚢** | merged #68→develop, #69→main; tag v2.4.0 | 06-10 | 3-lens Bengali-expert panel (×4 rules, vs 96.8k word-forms) approved ONLY «রম্ন»→«রু» (artifact-only; নিম্ন preserved), rejected 3 dangerous rules (ম্ন/েস্ন/ে্য/ণরে → deferred). `normalize_reorder_artifacts` at extraction (graph-only rebuild, NO re-conversion) + conversion. ~5.7k unsearchable Bengali words recovered (গ্রুপ/শুরু/পুরুষ/গুরুত্ব/গরু/দ্রুত); রম্ন artifacts 0, 0 regressions; +2 tests, 218 pass; venvs→2.4.0 |
| WP-88 | README redevelopment + de-stale all published surfaces | 5 | v2 | **DONE 🚢** | merged #70→develop, #71→main; tag v2.4.1 | 06-10 | docs/metadata only. 6-agent audit → full README rewrite + purge of v1 stale claims (audio/Whisper, mind-map, Ollama/local-LLM, apple-silicon, "vectors") across CITATION.cff/pyproject(+keywords)/manifest(+win32)/server.json/glama.json(+desc)/plugin/marketplace + commands/skill/installer docs + ACKNOWLEDGEMENTS + stale code docstrings; removed dead faster-whisper dep. GitHub description+topics refreshed. 4-agent adversarial review (verdict ship; caught 1 blocker, fixed). 218 tests; venvs→2.4.1. Repo cleanup = text-only (0 files deletable). |

| WP-89 | Token-free byte-caps: clamp `label` + convert char→byte caps in recall/overview (TF-1 Crit + TF-2 High) | 1/6 | v2 | **DONE** | merged wp-89→develop | 06-20 | `_clip_bytes`; label/text/docs/synopsis/theme byte-capped; Bengali store recall<100KB/overview<64KB; overlap-stop fallback; +5 tests |
| WP-90 | `resolve._norm` preserve Indic combining marks (RES-1 High) — stop force-merging distinct Bengali entities | 1/4 | v2 | **DONE** | merged wp-90→develop | 06-20 | _NORM_RE keeps Bengali block; fold Latin diacritics only; ভোলা≠ভালো/ঢাকা≠ঢাকি; +4 tests (first resolve coverage) |
| WP-91 | `.mcpb` Windows support (WIN-1 High) — ship launch.py + Windows command override | 1/2/5 | v2 | **DONE** | merged wp-91→develop | 06-20 | un-excluded launch.py; manifest platform_overrides.win32→python launch.py; `mcpb validate` passes; +2 tests. Real Windows start = Claude-Desktop-only (best-effort) |
| WP-92 | Archive rar/7z disk-fill bound (SEC-1 High) — live disk-usage monitor + reconcile SECURITY.md | 1/2 | v2 | **DONE** | merged wp-92→develop | 06-20 | `_run_extract_capped` kills+rolls-back on byte breach during extraction; SECURITY.md reconciled; +3 tests |
| WP-93 | Med code bundle: render note collision+clamp, _AUDIO_EXTS parity, cross-OS `newline=""` determinism, config docstring | 4/6 | v2 | **DONE** | merged wp-93→develop | 06-20 | note name via `_doc_key`; audio out of SUPPORTED_EXTS; LF text writers; recall_min_score doc=BM25; +2 tests |
| WP-94 | Med docs/de-stale: slash commands (BM25), README config table + AUTO_UPDATE=upstream, Dockerfile/CI dead-env, SECURITY.md + stale LLM comments | 5 | v2 | **DONE** | merged wp-94→develop | 06-20 | docs/text only; no behaviour change; residual stale-term scan clean |
| WP-95 | Round-2 hardening: make `_clip_bytes` total (non-str/neg-cap) + SECURITY.md doc nit | 3/6 | v2 | **DONE** | merged wp-95→develop | 06-20 | corrupt non-str field no longer raises across the MCP boundary; +2 tests |
| WP-96 | Release prep + publish v2.4.2 (version bump, CHANGELOG, tag, all channels) | 4/5/6 | v2 | **DONE 🚢** | PR #72→main; tag v2.4.2 (run 27869031283) | 06-20 | all 7 surfaces=2.4.2; train success; PyPI/GitHub+cosign/Homebrew/.mcpb/Docker verified; pip JSON-index edge transiently lagging at ship |

## Artifacts
`AUDIT.md` · `IMPROVEMENT_PLAN.md` · `ACCEPTANCE.md` · `RISKS.md` · `DECISIONS.md` · `SESSION_LOG.md` · `/CHANGELOG.md` · (later: `PUBLISH_MANIFEST.md`, `TEST_REPORT.md`, `SECURITY.md`)
